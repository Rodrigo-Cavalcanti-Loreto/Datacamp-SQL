-- Find the two errors in this code; the same error has been repeated twice.
-- Debug this code
SELECT film_id imdb_score num_votes
FROM reviews;


-- Debugged code
-- Faltava "," entre as colunas pedidas pelo SELECT
SELECT film_id, imdb_score, num_votes
FROM reviews;
